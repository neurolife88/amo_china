Вот очищенная и безопасная версия. Я заменил все конкретные домены, логины, пути на сервере и ссылки на репозиторий на нейтральные заглушки (например, `your-project`, `user@host`).

В таком виде этот текст можно смело публиковать в портфолио, на GitHub или отправлять заказчикам как пример технической документации.

---

# 🏥 Система "Встреча гостей" - Медицинский туризм в Китай

Веб-интерфейс для сотрудников медицинских учреждений, работающих с иностранными пациентами. Система интегрирована с CRM через n8n для автоматической синхронизации данных о прибывающих пациентах.

**Платформа разработки:** CURSOR AI-powered IDE

## 📋 Описание проекта

**🤖 AI-powered разработка в CURSOR IDE**

Система предназначена для управления потоками иностранных пациентов в клиниках на всех этапах. Разрабатывается с использованием CURSOR IDE для ускорения разработки через AI-ассистента.

**Основные модули:**

* **Встреча гостей** - отслеживание прибывающих пациентов
* **Пациенты на лечении** - управление пребыванием в клинике
* **Планирование отъезда** - организация обратного трансфера

### Ключевые возможности:

* ✅ **Real-time синхронизация** с CRM-системой
* ✅ **Ролевая система доступа** (Super Admin / Director / Coordinator)
* ✅ **Многобольничная архитектура** - поддержка нескольких филиалов
* ✅ **Адаптивный интерфейс** - mobile-first дизайн
* ✅ **Система аудита** - логирование всех изменений
* ✅ **Real-time уведомления** - мгновенные обновления данных

## 🛠️ Технологический стек

### IDE & Разработка

* **CURSOR** - AI-powered IDE с GitHub Copilot интеграцией
* **AI Assistance** - автоматическое написание кода и рефакторинг
* **.cursorrules** - настройки для контекста проекта

### Frontend

* **React 18** + **TypeScript** - основной фреймворк
* **Vite** - система сборки
* **Tailwind CSS** - стилизация
* **shadcn/ui** - компонентная библиотека
* **Radix UI** - примитивы интерфейса

### Backend & Database

* **Supabase** - PostgreSQL база данных + Authentication
* **Row Level Security (RLS)** - контроль доступа на уровне БД
* **Real-time subscriptions** - WebSocket соединения

### Интеграции

* **n8n** - автоматизация синхронизации между CRM и Supabase
* **AmoCRM** - источник данных о пациентах и сделках
* **Supabase Edge Functions** - серверная логика

## 🏗️ Архитектура системы

### Поток данных

```
CRM (Amo) → n8n → Supabase → Веб-интерфейс
   ↑                           ↓
   ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←

```

**Синхронизация:**

* **CRM → n8n → Supabase** - автоматический импорт новых сделок, контактов, билетов
* **Веб-интерфейс → Supabase** - редактирование полей координаторами
* **Supabase → n8n → CRM** - обратная синхронизация изменений (при необходимости)

### Центральная схема данных

Все данные организованы вокруг **сделки (deal)** - каждая сделка объединяет:

```
Сделка (Deal)
├── Контактная информация пациента (contacts)
├── Визовые данные (visas) 
├── Билеты прибытия (tickets_arrival)
├── Билеты отъезда (tickets_departure)
└── Привязка к клинике (clinics_directory)

```

## 👥 Ролевая система

### 🔴 Super Admin

* Полный доступ ко всем функциям системы
* Управление пользователями и правами доступа
* Просмотр аудита операций всех пользователей

### 🟠 Director

* Просмотр данных по всем больницам
* Аналитика и отчеты по всей сети клиник
* Мониторинг ключевых метрик

### 🟡 Coordinator

* Работа только со своей клиникой
* Редактирование данных пациентов
* Планирование размещения и отъезда
* Обновление статусов встречи

## 💻 Разработка и развертывание

### Основной способ - CURSOR IDE

**CURSOR** - это AI-powered IDE, оптимизированная для данного проекта:

* Склонируйте репозиторий: `git clone https://github.com/username/project-repo.git`
* Откройте проект в CURSOR IDE
* Используйте `.cursorrules` для контекста проекта

### Локальная разработка

**Требования:**

* **Node.js v22.x**
* **Git**
* **SSH доступ** к серверу (для деплоя)

1. **Клонирование репозитория**

```bash
git clone https://github.com/username/project-repo.git
cd project-name

```

2. **Установка зависимостей**

```bash
npm i

```

3. **Запуск**

```bash
npm run dev

```

### Продакшн развертывание

**Хостинг:** VPS / Cloud Hosting
**Веб-сервер:** Nginx

**Развертывание через Git:**

```bash
# Коммит изменений
git add .
git commit -m "Update production build"
git push origin main

# SSH подключение к серверу
ssh user@your-server.com

# На сервер: обновление кода и сборка
cd /path/to/project
git pull origin main
npm install
npm run build

```

### Домен и SSL

Домен подключен с автоматическим SSL сертификатом (Let's Encrypt / Hosting Provider SSL).

**Настройка Nginx (пример):**

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    # SSL сертификат
    ssl_certificate /path/to/ssl/cert;
    ssl_certificate_key /path/to/ssl/key;
    
    # Статические файлы React приложения
    location / {
        root /var/www/project/dist;
        try_files $uri $uri/ /index.html;
    }
}

```

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# Supabase
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# n8n интеграция
N8N_WEBHOOK_URL=your_n8n_webhook_endpoint
N8N_API_KEY=your_n8n_api_key

# Продакшн настройки
VITE_APP_URL=https://your-domain.com
VITE_ENVIRONMENT=production

```

### n8n Workflows

Настройка автоматизации синхронизации:

1. **CRM → Supabase** (основной поток):
* Webhook от CRM на создание/обновление сделок
* Трансформация данных в формат Supabase
* Вставка/обновление записей в БД


2. **Supabase → CRM** (обратная синхронизация):
* Trigger на изменения в таблицах
* Отправка обновлений статусов обратно в CRM



## 🛡️ Безопасность

### Row Level Security (RLS)

Политики доступа на уровне базы данных:

```sql
-- Координаторы видят только свою клинику
CREATE POLICY "coordinators_own_clinic" ON deals
FOR SELECT USING (
  clinic_name = (
    SELECT clinic_name FROM user_profiles 
    WHERE user_id = auth.uid()
  )
);

```

### Аудит операций

Все критические изменения логируются в таблицу `audit_log`, фиксируя кто, когда и какое поле изменил.

---

## 📄 Лицензия

Проект разработан для внутреннего использования медицинской сетью.

*Последнее обновление: 2025*
